/* Requires the Docker Pipeline plugin... */

pipeline {
    agent { docker { image 'node:19-alpine3.15' } }
    stages {
        stage('build') {
            steps {
                sh 'node --version'
            }
        }
    }
}